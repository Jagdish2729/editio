# Server-side AI configuration
# Copy these values into your local .env.local file.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Set to false when you want to test the real AI model.
# Keep true only for local fallback testing without a valid API key.
EDITIO_AI_FALLBACK=false
